import React from "react";
import styled from "styled-components";

const Logo = () => {
  return (
    <LogoComponent>
      <h1>LOGO</h1>
    </LogoComponent>
  );
};

export default Logo;

const LogoComponent = styled.div`
  margin-right: 4rem;
`;
